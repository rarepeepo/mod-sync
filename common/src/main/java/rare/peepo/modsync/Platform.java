/*
package rare.peepo.modsync;

import dev.architectury.injectables.annotations.ExpectPlatform;
import java.nio.file.Path;

//
// The methods are replaced at compile-time with their counter-parts of
// the respective loader's PlatformImpl class.
// 
// We don't use this class at all at the moment, though. It's just here
// in case we need it in the future.
// 
// Since we don't use it, we can bundle our mod in a single .jar file
// that contains both fabric and forge code instead of having to have
// separate .jars for each mod loader.
// 
//

public class Platform {
    //
    // Not used.
    // 
    // We figure out the game directory from the location of our mod's
    // JAR file, see Util.getGameDirectory.
    //
    @ExpectPlatform
    public static Path getGameDirectory() {
        // Just throw an error, the content should get replaced at runtime.
        throw new AssertionError();
    }
    
    //
    // Not used.
    // 
    // Gets the command line arguments used to launch the game.
    // 
    // @return
    //  An array of the command line arguments such as --gameDir, --assetsDir etc.
    //  that were passed by the launcher program to the mod launcher.
    //
    @ExpectPlatform
    public static String[] getLaunchArguments() {
        // Just throw an error, the content should get replaced at runtime.
        throw new AssertionError();
    }
}
*/
